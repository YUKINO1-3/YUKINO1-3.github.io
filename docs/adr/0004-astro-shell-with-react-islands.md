# Use Astro as the static shell with React islands

Astro owns layouts, routes, and validated content collections, while React is used only for stateful interactive experiments. This retains the requested React and Vite ecosystem while avoiding a site-wide client runtime and custom pre-rendering machinery for an admissions portfolio whose dominant concern is durable, readable static content.
